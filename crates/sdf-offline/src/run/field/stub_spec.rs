/// One stub tile's expectation in a scaffolded convert scenario.
pub(crate) struct StubSpec {
    pub(crate) x: u32,
    pub(crate) y: u32,
    pub(crate) mip_count: u32,
    pub(crate) constant: bool,
}
